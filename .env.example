# Local dev: point directly to the backend
# VITE_API_BASE_URL=https://commerceedge.onrender.com/api/v1
#
# Production (Vercel): use the proxy rewrite — leave this UNSET or set to /api/v1
VITE_API_BASE_URL=/api/v1
